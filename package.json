{
  "name": "cb",
  "version": "1.0.0",
  "description": "A simple HTTP Web API server.",
  "main": "index.js",
  "scripts": {
    "start": "tsc *.ts --lib ES2015,dom --outDir dist && nodemon -r dotenv/config ./dist/index.js --watch './*'",
    "lint": "npx eslint . --fix && npx prettier --write '**/*.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Angelo",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.0.3",
    "dotenv": "^16.5.0",
    "eslint": "^9.29.0",
    "express": "^5.1.0",
    "mongodb": "^6.17.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.0",
    "typescript-eslint": "^8.34.1"
  }
}
